os:
  - osx
  - linux

language: generic

sudo: required
dist: trusty

osx_image: xcode9.3

script:
  - eval "$(curl -sL https://swift.skrundz.ca/ci)"
  - travis_wait 50 eval "$(curl -sL https://swift.skrundz.ca/codecov)"
